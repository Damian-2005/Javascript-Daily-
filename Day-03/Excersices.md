**Camelizing Bot.**

This bot takes a provided dash-case/kebab-case string and turns it into a form.

Algo;

-split the string with using the dashes in between.
-use map to access the elements, capitalize first element and adds the rest with it.
-join to join thestrings together.

