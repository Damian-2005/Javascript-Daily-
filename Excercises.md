__Second Task__

A function that takes an array of numbers + 2 other numbers (a & b) as an argument.

- Returns the array if there are no other arguements but the array

- Returns an array of numbers that meets the condition a<= x >=b

